<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?attr/colorSurface">
    <!-- Dùng colorSurface thay vì #1C1C1E để hỗ trợ theme sáng/tối -->

    <!-- 1. Caption (Tiêu đề cảm xúc) -->
    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/textCaption"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="32dp"
        android:layout_marginBottom="24dp"
        android:gravity="center"
        android:text="@string/angry_it_s_ok"
        android:textColor="?attr/colorOnSurface"
        android:textStyle="bold"
        style="@style/TextAppearance.Material3.HeadlineSmall"
        app:layout_constraintBottom_toTopOf="@id/card_image_container"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <!-- 2. Container Ảnh (Dùng CardView để bo góc ảnh) -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/card_image_container"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:cardCornerRadius="28dp"
        app:cardElevation="0dp"
        app:strokeWidth="0dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintDimensionRatio="1:1"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintWidth_percent="0.85">

        <ImageView
            android:id="@+id/postImageView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:contentDescription="@null"
            android:scaleType="centerCrop"
            tools:srcCompat="@tools:sample/backgrounds/scenic" />

    </com.google.android.material.card.MaterialCardView>

    <!-- 3. Timestamp (Chip hiển thị thời gian) -->
    <!-- Dùng Chip hoặc CardView thay vì Button để nhẹ hơn và đúng ngữ nghĩa -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/cardTimestamp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        app:cardBackgroundColor="?attr/colorSurfaceContainerHigh"
        app:cardCornerRadius="16dp"
        app:cardElevation="0dp"
        app:strokeWidth="0dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/card_image_container">

        <com.google.android.material.textview.MaterialTextView
            android:id="@+id/tvTimestamp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingHorizontal="16dp"
            android:paddingVertical="8dp"
            android:text="@string/timeline"
            android:textColor="?attr/colorOnSurfaceVariant"
            style="@style/TextAppearance.Material3.LabelLarge" />
    </com.google.android.material.card.MaterialCardView>

    <!-- 4. Nhóm người xem (Reactions/Viewers) -->
    <LinearLayout
        android:id="@+id/layoutAvatarGroup"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:gravity="center"
        android:orientation="horizontal"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/cardTimestamp">

        <!-- Icon nhỏ -->
        <ImageView
            android:layout_width="16dp"
            android:layout_height="16dp"
            android:layout_marginEnd="6dp"
            app:tint="?attr/colorOnSurfaceVariant"
            tools:ignore="ContentDescription" />
        <!-- Lưu ý: Bạn cần có icon vector ic_group_24 hoặc tương tự -->

        <com.google.android.material.textview.MaterialTextView
            android:id="@+id/textAvatarGroup"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="1"
            android:text="@string/timeline_2"
            android:textColor="?attr/colorOnSurfaceVariant"
            style="@style/TextAppearance.Material3.BodySmall" />
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
