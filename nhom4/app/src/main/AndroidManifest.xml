<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"/>

    <uses-feature android:name="android.hardware.camera" android:required="true" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.page.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- LoginActivity không còn là Activity khởi chạy -->
        <activity
            android:name=".ui.page.auth.LoginActivity"
            android:exported="false" />

        <activity
            android:name=".MainActivity"
            android:exported="false" />
        <activity android:name=".ui.page.auth.AddFriendActivity" android:exported="false" />
        <activity android:name=".ui.page.calendar.ProfileActivity" android:exported="false" />
        <activity android:name=".ui.page.auth.CreateUsernameActivity" android:exported="false" />
        <!-- Các activity khác... -->
        <activity android:name=".ui.page.auth.RegisterActivity" android:exported="false" />
        <activity android:name=".ui.page.chat.ChatActivity" android:exported="false" android:windowSoftInputMode="adjustResize" />/>
        <activity android:name=".ui.page.calendar.StoryAllActivity" android:exported="false" android:windowSoftInputMode="adjustResize" />/>

        <!-- Widget Provider -->
        <receiver
            android:name=".ui.page.widget.ActivityWidgetProvider"
            android:exported="false">

            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/activity_widget_info" />
        </receiver>
        <service
            android:name=".ui.page.widget.ActivityWidgetService"
            android:permission="android.permission.BIND_JOB_SERVICE"
            android:exported="false" />

    </application>

</manifest>
